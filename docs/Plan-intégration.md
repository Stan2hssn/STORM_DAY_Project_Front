# Plan d’intégration (archivé ici)

**Tout le contenu utile pour ce dépôt front** (endpoints, WebSocket, backlog, optimisations) est regroupé dans le **[README à la racine](../README.md)**.

Les sections longues (dépannage k8s, exemples JSON complets, checklists gateway dans un autre repo) n’y sont pas recopiées : les conserver dans la doc du **monorepo backend / gateway** si nécessaire.
